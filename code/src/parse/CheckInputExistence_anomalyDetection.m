function CheckInputExistence_anomalyDetection(pars)

% if ~exist(pars.paths.folders.pathToGndTruth,'dir')
%     warning('No ground truth detected');
% end
% % Commented out because it's now in the wrong order to check this (the
% user may have just asked us to make the default file)
% if ~exist(pars.cuboidFeatsFile,'file') && strcmpi(pars.rawFeatType, 'cuboidBin')
%     error('Your cuboidFeatsFile does not exist: %s\n',pars.cuboidFeatsFile);
% end

end
